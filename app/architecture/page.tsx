import { ArrowDown, Bot, Braces, Database, Server, ShieldCheck } from "lucide-react";
const stages = [{icon:<Braces/>,title:"Next.js UI",text:"Free-text intake, result visualization and dashboard."},{icon:<Server/>,title:"Server API",text:"Secret isolation, request validation and orchestration."},{icon:<Bot/>,title:"Gemini",text:"Low-temperature structured extraction with a strict response schema."},{icon:<ShieldCheck/>,title:"Zod boundary",text:"Reject malformed AI output before it reaches business data."},{icon:<Database/>,title:"Supabase PostgreSQL",text:"Claims, AI metadata, reviewer state and timestamps."}];
export default function ArchitecturePage(){return <section className="page-section"><div className="page-head"><span className="eyebrow">SYSTEM DESIGN</span><h1>AI is one component — not the system.</h1><p>The design isolates model risk behind validation, keeps secrets server-side, and deliberately requires human validation before downstream action.</p></div><div className="architecture">{stages.map((s,i)=><div key={s.title}>{i>0&&<ArrowDown className="arch-arrow"/>}<article className="glass arch-card"><span className="arch-icon">{s.icon}</span><div><h2>{s.title}</h2><p>{s.text}</p></div></article></div>)}</div></section>}
